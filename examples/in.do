let msg = "Hello, world!"
let msg_has_hello = "Hello" in msg
println(msg_has_hello)

let nums = 1..10
let nums_has_5 = 5 in nums
println(nums_has_5)

let user = { name: "Alice", age: 30 }
let user_has_name = "name" in user
println(user_has_name)
